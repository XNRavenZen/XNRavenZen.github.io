// const { showLoading } = require('$utils/allLoading');
import { showLoading } from '../utils/allLoading';
const state = {
  loadingFunc: showLoading
}

const modules = {
}

const mutations = {
}

const actions = {
}

export default {
  state,
  modules,
  mutations,
  actions,
}